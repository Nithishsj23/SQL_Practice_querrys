----Find products whose sales are higher than the average sales within their own category----
select
*
from
(
select 
category
product,
sum(sales) as total_sales,
avg(sum(sales)) over() as avg_sales
from orders as o

left join products as p
on o.productid = p.productid

group by product, category)t

where total_sales>avg_sales

