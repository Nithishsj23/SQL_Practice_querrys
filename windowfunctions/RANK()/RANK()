SELECT 

    orderdate,
    orderid,
   productid,
    sales,

     ROW_NUMBER() over (order by sales DESC) as salesbyrank_row,

     RANK() over (order by sales DESC) as salesbyrank_rank

    FROM orders;
