-----Find categories whose total revenue is below the average revenue of all categories.----------

select 
*
from
(
 select 
category,
sum(sales) as totalrevenue,
avg(sum(sales)) over () as avgrevenue
from orders as o
left join 
 products as p
on o.productid = p.productid
group by category
)t
where totalrevenue < avgrevenue;
