--- rank customers based on sales--

select
customerid,
total_sales,
rank()over(order by total_sales desc) rankbysales
from
(
select
customerid,
sum(sales) as total_sales
from orders
group by customerid
)t
