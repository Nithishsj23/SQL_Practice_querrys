--- Show all customer details and total sales amount generated by each customer

select * from customers as c 
left join
(
select 
customerid,
sum(sales) as total_sales
from orders
group by customerid) as o
on c.id = o.customerid
